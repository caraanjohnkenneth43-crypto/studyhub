/**
 * Chat password gate helpers — no longer used (all rooms are public).
 * @checkTypes
 */

export function hasStoredPassword() { return null }
export function savePassword() {}
export function getStoredPasswords() { return {} }
